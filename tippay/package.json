{
  "name": "tippay",
  "version": "0.1.0",
  "private": true,
  "description": "TipPay — Indian UPI Tipping & Services Platform",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:generate": "pnpm --filter @tippay/database generate",
    "db:migrate": "pnpm --filter @tippay/database migrate:dev",
    "db:seed": "pnpm --filter @tippay/database seed",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
